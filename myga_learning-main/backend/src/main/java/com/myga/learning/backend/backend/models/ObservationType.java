package com.myga.learning.backend.backend.models;

/** Category of a teacher's observation about a student. */
public enum ObservationType {
    ACADEMIC,
    BEHAVIOR,
    PARTICIPATION,
    HOMEWORK,
    POSITIVE_FEEDBACK,
    CONCERN
}
